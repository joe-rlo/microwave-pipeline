# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/
*.whl
.venv/
venv/
env/

# Runtime data
*.db
*.db-journal
*.db-wal
*.db-shm

# Secrets
.env
.env.*
!.env.example

# Workspace (user's personal data — not committed)
workspace/

# Prompts directory — local-only. The skills system (workspace/skills/)
# is the canonical place for shipped prompt templates; anything in
# prompts/ is treated as personal scratch.
prompts/

# Personal essays / drafts that sometimes land at the repo root. Add
# explicit excludes here for any new ones rather than relying on a
# blanket pattern that could trap shipped docs (README.md etc.).
/determinism-and-llms.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Lint / test caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
htmlcov/
.coverage

# Claude Code
.claude/
